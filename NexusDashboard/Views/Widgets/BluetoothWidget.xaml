<UserControl
    x:Class="NEXUS.Views.Widgets.BluetoothWidget"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border CornerRadius="20" Padding="22" BorderThickness="1.5" BorderBrush="{StaticResource GlassBorder}"
            Background="{StaticResource GlassBackground}"
            PointerEntered="Border_PointerEntered" PointerExited="Border_PointerExited"
            VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- CARD HEADER -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                    <Image Width="20" Height="20" VerticalAlignment="Center">
                        <Image.Source>
                            <SvgImageSource UriSource="ms-appx:///Assets/Icons/bluetooth-icon.svg" />
                        </Image.Source>
                    </Image>
                    <TextBlock Text="BLUETOOTH DEVICES" FontSize="10" FontWeight="Medium" CharacterSpacing="200" 
                               Foreground="#40FFFFFF" VerticalAlignment="Center" />
                </StackPanel>

                <!-- Count Text -->
                <TextBlock x:Name="DeviceCountText" Grid.Column="1" FontSize="10" Foreground="#40FFFFFF" HorizontalAlignment="Right" VerticalAlignment="Center" />
            </Grid>

            <!-- Device List Container (Horizontal Wrap Cards) -->
            <ItemsControl x:Name="DeviceListPanel" Grid.Row="1">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
